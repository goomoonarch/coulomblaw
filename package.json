{
  "name": "simulador-cargas",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --base=/coulomblaw/",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages-deploy": "^0.5.1",
    "vite": "^5.2.0"
  }
}
